{
    "Logging": {
        "LogLevel": {
            "Default": "Warning"
        }
    },
    "ConnectStrings": {
        "Mongo":"mongodb://$(MONGO_USER):$(MONGO_PWD)@$(MONGO_HOST):@$(MONGO_PORT)"
    }
}